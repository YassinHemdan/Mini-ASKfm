spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ASKfm
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    baselineOnMigrate: true